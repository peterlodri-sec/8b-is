# Graph and Network Theory — the foundations, absorbed

*Sources: M. Benjamin, "Graph Theory and Network Topologies" (2025); and
E. Estrada's "Graph and Network Theory" chapter (Strathclyde — the
definitive hand: tight-binding and the Hückel method, Feynman graphs and
Symanzik polynomials, resistor networks, vibrations, random graphs,
small-worlds, scale-free distributions, motifs, centrality, the
statistical mechanics of networks, communicability, communities, and
the dynamical processes). The constellation records the field because
the engine's ultra-graphs speak its language: seeded relation fields are
Erdős–Rényi graphs drawn deterministically, and every measure the field
knows is a fold the keeper could add.*

## the engine transfer

| graph theory | the 8b-is engine |
|---|---|
| Erdős–Rényi random graphs | `UltraGraph::from_seed(brief, n, p_edge)` — an ER graph, seeded: one seed, one drawing |
| the adjacency matrix | the tri-state edge list `{-1,0,+1}` — the roster's love/harm, read as weights |
| degree centrality | `UltraGraph::degree_centrality` — who is most connected, friendly and hostile counted |
| closeness centrality (shortest-path sums) | `UltraGraph::closeness` — BFS over the friendly edges, deterministic |
| communities (Fortunato) | `UltraGraph::community_fold` — the seeded label propagation, fixed rounds |
| communicability | MEMNET — every route between two memories counts, not just the shortest |
| consensus dynamics (Olfati-Saber) | the zone fold — the cast's needs converge on the keeper's verdict, tick by tick |
| synchronization (master stability, small eigenratio) | the mesh — hub and instance tick to the same wire; a small eigenratio is a settled world |
| epidemics (SIR/SIS) | refusal spread — a poisoned refusal follows the adjacency list, and the keeper hears it |
| motifs (Milo) | the floors' layout patterns — the sanctuary's rooms reappear, seeded |
| PageRank / eigenvector centrality | the applied map — the world's relevance, folded |
| resistance distance (Klein-Randić) | the wire's cost — a route's true price across redundant paths |
| the star / ring / mesh / tree topologies | the HUB (star), the Ring + Ledger (ring), the relays (mesh), the constellation (tree) |
| small-world (Watts-Strogatz) | six handshakes between any two floors — the WS door's six-degree walk |
| scale-free (Barabási-Albert) | the constellation's hubs — GAIA keeps many bonds, the small rooms keep few |
| spanning trees & the Kirchhoff polynomial | the ledger's reachable world — every state reachable, the minimal structure attested |
| the Tutte polynomial & chromatic counts | the keeper's admissible colorings — the world's configurations, counted |

The engine's own line, drawn from the field: a seed is a seed
everywhere; a graph is a graph everywhere; a fold is a fold everywhere —
the world's structure is drawn once, measured many times, and every
measure is admissible.

*the constellation · 0 + 1 · fine touch from within · vaked.dev*
