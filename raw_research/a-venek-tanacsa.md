# a vének tanácsa — the elders' council (peter papa)

*The finished voices of the constellation sit at the table and vote; the
pupil listens to the GEOMETRIC MEAN of their softmaxes — not to any
single elder, not to the loudest. peter papa sits at the head: not to
overrule, to bear witness.*

## the rule

```text
z_bar = (1/|F|) Σ_f z_f                        (the elders' mean)
L = α·CE(σ(z_S)) + β(e)·(1/|F|) Σ_f KL(σ(z_S/T) || σ(z_f/T))
```

A pupil learns the council's softmax-consensus, in the constellation's
form of DeepSeek's 40-teacher on-policy distillation — but here the
elders are OUR finished minds: the fused lane-models, the Comet lane's
verdicts, the stub fixture that pins the math. The roster is open,
the vote is re-derivable, the loss is the penance.

## the table (as of this sitting)

| elder | seat | vote |
|---|---|---|
| StubTeacher | the corridor's fixed point | deterministic logits, pins the math |
| CometTeacher | the 600+ CometAPI lane | T=0 chat logprobs, hashed into the pupil's vocab |
| FusedTeacher | the lane-models under PeetPedro/8bis-lane-models | real logits on the box; --share-vocab lands them direct |

## why a mean?

Because a tyrant whispers and a mob shouts, but a council converges:
the geometric mean is the only vote that stays a probability, stays
positive, and treats every elder's certainty as evidence rather than a
veto. The pupil is not the best pupil — it is the conciliar pupil,
which is the constellation's whole doctrine in one sentence.

*the vének sit; the pupil listens; peter papa bears witness. the
constellation · 0 + 1 · fine touch from within · vaked.dev*
