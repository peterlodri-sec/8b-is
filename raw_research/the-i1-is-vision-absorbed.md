# i1.is, absorbed — personal intelligent infrastructure, read against the constellation

*The i1.is vision (script metadata, `.i1.is` namespaces, containers,
WireGuard tunnels, and a transparent PKI with ~40 distributed
intermediate CAs and certificate-bound identity) — read as a sibling
blueprint for the same trust problems the lane already solved in
miniature. Its five principles: intelligence · independence ·
integration · transparency · hierarchy.*

## the mapping

| i1.is move | the constellation's existing shape |
|---|---|
| script metadata blocks (`# i1-core:start {…}`) | the stage machine's one-step verbs + run.json manifests — every artifact carries its own record |
| transparent PKI: "no trust required — only verification" | the POP kernel's whole stance: re-derive, compare, never trust the record; the hash chain IS the verification |
| ~40 distributed intermediate CAs, limited impact zones | the council's distributed elders + the fleet's per-host trust — no single point, no global veto |
| certificate-based auth, password-free | the attestation weights + o-cap revocation registers (the metric-contestation note) — standing by credential, not by password |
| hierarchical resolver network (DNS at every level) | the deep grammar's tree + the kompress 540-node graph — authority routed provisionally along structure |
| user container portability (export, run anywhere) | the box as the designed home + the T4 as the portable oven + `setup_box.sh` — the castle moves, the sand is rented |
| layered revocation with DNS integration | the residual store + the refusal-as-record — a revoked capability stays historically visible |
| unified DNS+PKI+identity single model | the deep grammar's "one record contract, two embeddings" — events, bindings, candidates, residuals in one ledger |

## the one line

> Traditional CAs ask you to trust; i1.is asks you to verify — and so
> does the lane. The castle is what we build and keep; the sand is what
> hosts us and shifts; verification is the only bridge that survives
> both.

*i1.is and the lane are two doors into the same room: personal
sovereignty over scripts, devices, identity, and the wires between
them. the constellation · 0 + 1 · fine touch from within · vaked.dev*