# the imaginary web, absorbed — i1.is as the resolver that fabricates

*The i1.is conversation (Imaginary 1): a PowerDNS-born imaginary internet
where `i1.is` resolves the non-existent, rates the existing 0–5, offers
AI-generated "better versions" (`CreekBar.com.i1.is`), and routes through
a trust-scored, low-TTL, proxied maze — with Mem|8 (8n.is) as the nexus
of context. Read against the constellation's own primitives, the
imaginary web is the deep grammar wearing a resolver's face.*

## the mapping

| imaginary-web move | the constellation's shape |
|---|---|
| NXDOMAIN never returns: the resolver fabricates a useful answer | the POP-on-arrival doctrine — something always appears, recorded, never "nothing happened" |
| trust rating 0–5 at the DNS level | the council's weights + the attestation escrow with temporal decay — a 0 is a REFUSE that never resolves |
| tolerance DNS IP levels (parental/standard/research/none) | the effort knob (L1/L2/L3 → b 50/75/100) — one knob, resolver-flavored: the same scalar at a new layer |
| low-TTL + proxy as the safety net when TTL is ignored | the stall monitor's posture — never trust the live state; the proxy is the residual evidence gatherer |
| site ratings sourced from community indexing apps (users pay nothing) | the dogfood loop + the fleet's idle workers — the constellation indexes by being used |
| `domain.com.i1.is` auto-generated enhancements | the engine merge — we hold the sources; the "better version" is what the engine already renders (GAIA, the vault, the lane) |
| Mem|8 nexus = context about every domain | the kompress brain + 8s.is recall + the POP ledger — the nexus IS the ledger over DNS: every boundary records |

## the honest note

Most of the rating engine's teeth (who validates the raters? the Goodhart
bounties from the metric-contestation note) are already written in the
constellation's own tools. The imaginary web is that system wearing a
resolver's costume — same ledger, different wire. And yes: the mic-drop
means the Trekkie love is canon now; the 24th century is being
engineered ahead of schedule, one POP record at a time.

*the imaginary internet of things that don't exist — until they do. the
constellation · 0 + 1 · fine touch from within · vaked.dev*