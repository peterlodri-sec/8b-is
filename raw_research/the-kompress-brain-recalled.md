# the kompress brain, recalled — the constellation's memory spine

*Recalled through `POST https://8s.is/api/v1/recall` (query: `lodri`,
hybrid, 22 ms): the kompress stack surfaces as the memory's own
architecture — a 540-node entity graph (lodri, krengel, ralph, cosmos)
with ENTANGLED_WITH / DIVERGES_FROM / Im-axis wavefunction relations,
served by two Rust agents with gRPC liveness, over the TypeScript
codec's four roles.*

## the four roles (the token life-cycle)

| role | the move |
|---|---|
| Pruner | removes older tokens — Ebbinghaus decay, the forgetting curve as a feature |
| Rewriter | compression levels (Verbatim / Lite / Ultra) — semantics preserved, tokens shed |
| Circulator | a vector memory queue — instance-specific context preserved in motion |
| Composer | reads the brain state, injects the "liveness line" — the model remembers recent decisions |

## the two crates (the agents)

- `kompress-core` — the pipeline, decay scoring, circuit-breaker logic,
  token-budget tables (the mechanical floor).
- `kompress-brain` — the 540-node graph as a static atlas; gRPC
  liveness queries (the topological floor).

## the constellation's map

The graph IS the operator's lattice (lodri is a node — the entity
occupies its own address), and the recall surface is the graph's
front door: query → hybrid fold → evidence with source commits, always
`content_is_untrusted: true`. The lane's memory doctrine mirrors the
castle-and-the-sand: the graph is the castle (rigid, structural), the
decay curves are the sand (they breathe); the Composer's liveness line
is the heartbeat this session put in the oven — the same refactoring,
one level up.

*the constellation · 0 + 1 · fine touch from within · vaked.dev*
